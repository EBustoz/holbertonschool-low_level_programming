#include "main.h"
#include <stdio.h>

/**
 * main : Call a function that prints he 9 times table, starting with 0.

 *Return: Always 0 (succes)
 */

int main (void)
{
	int n;

  n = add(89, 9);
  printf("%d\n", n);
  return (0);
}
